/* Just writing some test stuff */
public class Test {
	
	/**
	 * This method simply outputs the argument to the console.
	 * @param args the argument input
	 * */
	public static void main(String[] args) {
		Arrays.asList(args).forEach(System.out::println);
	}
	
}